spring.application.name=casopractico2

# Puerto del servidor (puerto 78 como indica el ejercicio)
server.port=90

# Formato de log
logging.pattern.dateformat=hh:mm

# Desactivar banner de Spring
spring.main.banner-mode=off

# Configuración Thymeleaf
spring.thymeleaf.cache=false

# Configuración MySQL - Conexión a la base de datos plataforma
spring.datasource.url=jdbc:mysql://localhost:3306/plataforma?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=myeseqlo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.type.descriptor.sql.BasicBinder=TRACE